A couple of vanilla JS array methods would be really useful for organzing the data. Think about what you need to do with the data and which methods would best suited for the tasks at hand. There are two main tasks with two corresponding methods. 

🚨 Spoiler alert! ️️Want another hint? ⬇️ Scroll down to line 50 to get one — but consider thinking through this on your own a bit more first! 🚨














































The two main tasks are *filtering* the data so it just has the bands playing on the given date and *sorting* the data so that the bands are ordered by importance. In other words, you can use the filter and sort methods! 